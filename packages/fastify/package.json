{
  "name": "@tenantscale/fastify",
  "version": "0.1.1",
  "description": "Fastify middleware adapter for TenantScale — API key auth, plan enforcement, rate limiting, audit logging",
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "clean": "rm -rf dist",
    "lint": "oxlint src/"
  },
  "dependencies": {
    "@tenantscale/sdk": "workspace:*"
  },
  "peerDependencies": {
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "fastify": "^5.12.1",
    "typescript": "^7.0.2"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/TenantScale/sdk.git",
    "directory": "packages/fastify"
  },
  "engines": {
    "node": ">=22"
  }
}
